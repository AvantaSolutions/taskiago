import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.avantamarketing.taskiago',
  appName: 'Taskiago',
  webDir: 'www/browser'
};

export default config;
